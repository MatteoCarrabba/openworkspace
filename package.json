{
  "name": "openworkspace",
  "version": "1.0.0",
  "private": true,
  "description": "OpenWorkspace v1 \u2014 workspace substrate: projects, primitives, automations over a plain filesystem.",
  "bin": {
    "projects": "dist/src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc && npm run build:client && mkdir -p dist/src/dashboard && cp src/dashboard/client/dist/index.html dist/src/dashboard/index.html",
    "build:client": "npm --prefix src/dashboard/client run build",
    "dev": "tsc --watch",
    "test": "npm run build && node --test \"dist/tests/**/*.test.js\""
  },
  "dependencies": {
    "smol-toml": "^1.3.1",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
