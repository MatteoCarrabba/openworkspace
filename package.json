{
  "name": "openworkspace",
  "version": "1.0.0",
  "private": true,
  "description": "OpenWorkspace v1 \u2014 workspace substrate: projects, primitives, automations over a plain filesystem.",
  "bin": {
    "projects": "dist/src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc && mkdir -p dist/src/dashboard && cp src/dashboard/index.html dist/src/dashboard/index.html",
    "dev": "tsc --watch",
    "test": "npm run build && node --test \"dist/tests/**/*.test.js\""
  },
  "dependencies": {
    "smol-toml": "^1.3.1",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
