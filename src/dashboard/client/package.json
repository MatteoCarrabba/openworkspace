{
  "name": "openworkspace-dashboard-client",
  "version": "1.0.0",
  "private": true,
  "description": "React + Vite source for the OpenWorkspace dashboard. Builds to a single self-contained index.html that src/dashboard/server.ts serves unchanged.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^6.0.0",
    "typescript": "^5.7.0",
    "vite": "^8.0.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
